<h1>The Cart.</h1>
    <li ng-repeat="product in current_cart">
       {{product.id}} - {{product.name}} - {{product.price | currency}} - Quantity: {{product.quantity}}
       <a href ng-click="changeItemQuantity(product,'up')">Increase</a>
    </li>
    <h3 >{{totalItems}}</h3>