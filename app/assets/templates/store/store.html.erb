<h1>This Store.</h1>
<ul>
    <li ng-repeat="product in products">
        {{product.name}} - {{product.price}} - {{product.description}}
        <a href ng-click="addItemtoCart(product)">Add</a>
    </li>
</ul>